[project]
name = "module2-agents-md"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vadim", email = "9073321@mail.ru" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
module2-agents-md = "main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
